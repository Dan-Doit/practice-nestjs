type Query {
  all_users: [User!]!
}

type Query {
  one_user(email: String!): User!
}

type Mutation {
  add_user(
    email: String!
    password: String!
    avatar: String
    name: String!
    token: String!
  ): String!
}
